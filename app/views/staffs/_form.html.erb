<%= form_with(model: staff, local: true) do |form| %>
  <% if staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
      <% staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :staff_name %>
    <%= form.text_field :staff_name, id: :staff_staff_name %>
  </div>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.number_field :staff_id, id: :staff_staff_id %>
  </div>

  <div class="field">
    <%= form.label :staff_age %>
    <%= form.number_field :staff_age, id: :staff_staff_age %>
  </div>

  <div class="field">
    <%= form.label :staff_subject %>
    <%= form.text_field :staff_subject, id: :staff_staff_subject %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
